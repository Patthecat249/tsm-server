# Dies ist das erste Play
- name: "Installiere einen Server"
  hosts: tsm
  gather_facts: true
  vars_files: 
    - "../vars/central_vars.yaml"
  tasks:
    - name: "# --- 00 - Install prerequisites and software nessesary programs"
      yum: 
        name: "{{ item }}"
        state: "present"
      with_items:
        - nfs-server


    - name: "# --- 01 - Erstelle Arbeitsverzeichnisse"
      file: 
        path: "{{ item }}"
        #mode: ""
        #owner: ""
        #group: ""
        state: directory
        recurse: true
      with_items:
        - "{{ mount_path_tsm }}"
        - "{{ mount_path_cdrom }}"
        - "{{ dir_work }}{{ dir_tsm }}{{ dir_tsm_extracted_install }}"

# Mounte NFS-Server-Verzeichnis zum Kopieren der Kickstart-Datei
    - name: "# --- 02 - Mounting NFS-Share"
      mount:
        fstype: nfs
        opts: "nfsvers=3,nolock"
        dump: "0"
        passno: "0"
        state: mounted
        src: "{{ nfs_server_name }}:{{ nfs_path }}"
        path: "{{ mount_path_tsm }}"

# Copy BIN-File
    - name: "# --- 03 - Copy BIN-File from NAS to {{ dir_work }}{{ dir_tsm }}{{ dir_tsm_extracted_install }}"
      copy:
        src: "{{ mount_path_tsm }}{{ nfs_path_bin_file }}"
        dest: "{{ dir_work }}{{ dir_tsm }}{{ dir_tsm_extracted_install }}{{ bin_file_name }}"
        remote_src: yes

# Extract the BIN-File
    - name: "# --- 04 - Extract BIN-File"
      shell: "{{ dir_work }}{{ dir_tsm }}{{ dir_tsm_extracted_install }}{{ bin_file_name }}"

# Erstelle den TSM-User
    - name: "# --- 05 - Erstelle den TSM-User {{ tsm_username }}"
      user:
        name: "{{ tsm_username }}"
        comment: "Dies ist der TSM-Installationsbenutzer"
        uid: 2222
        group: 1111
    

  
